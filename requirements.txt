# Core
pyyaml>=6.0
python-dotenv>=1.0.0

# GBrain markdown storage
python-frontmatter>=1.1.0

# Embeddings
openai>=1.12.0
numpy>=1.26.0

# SQLite for vector index
# (using stdlib sqlite3, no extra package needed)

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
